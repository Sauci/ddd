{
  "$schema": "../../schemas/ddd_constants.schema.json",
  "constants": [
    { "name": "TREND_SAMPLES", "value": 16,
      "description": "sample slots of a pressure trend buffer, a device wide size no single component owns" }
  ]
}
