# The repository is checked out on Windows but its scripts run inside a linux container:
# a CRLF in docker/compile.sh makes the shebang fail with "bash\r: No such file or directory".
* text=auto eol=lf
*.png binary
*.pdf binary
